name: testing

on:
  push:
    branches: 
      - main
    
jobs:
  testing:
    runs-on: ubuntu-20.04

    steps:
      - name: greeting
        run: echo "Hello Prashanth GR"
      - name: checking resources
        run: nproc 
      - name: Say Byee
        run: "echo See you again"
      